package checks.checkstyle.javadocCommentMethodCheckTest;

public class MethodWithTwoEmptyLines {
    
    /**
     * @param watchService the watch service
     * 
     * @param watchedDir the base directory, watched by the watch service
     * 
     * 
     * @param registeredKeys the registered keys
     */
     public void AbstractWatchQueueReader(WatchService watchService, Path watchedDir, Map<WatchKey, 
     Path> registeredKeys){
        
     }
}
